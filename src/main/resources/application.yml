spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reader
    username: admin
    password: password
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  show-sql: true
jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub
admin:
  username: admin
  password: password